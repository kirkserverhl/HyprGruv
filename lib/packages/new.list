# Hyprgruv potential packages — optional, not auto-installed during install.sh
# Install on demand: bash ~/.hyprgruv/sync-packages.sh --new-only
# Excluded: overskride, hypremoji/smile, ghostty-*-git variants (official ghostty → setup defaults wizard), tmux-resurrect,
#           jetbrains-mono, ttf-nerd-fonts-symbols
# Lines starting with # are ignored.

easyeffects