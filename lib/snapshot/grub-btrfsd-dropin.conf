# Installed to /etc/systemd/system/grub-btrfsd.service.d/hyprgruv.conf
# Timeshift snapshots are not under /.snapshots (that path is Snapper).
# --timeshift-auto watches Timeshift's live snapshot directory.
[Service]
ExecStart=
ExecStart=/usr/bin/grub-btrfsd --syslog --timeshift-auto
