# Managed by Hyprgruv apply-machine-profile.sh (laptop)
# logind ignores the switch; hyprgruv-lid-watch.service owns the 5-minute
# unlocked grace, then lock + hibernate (desk dock with an external stays on).
[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
