---
id: 2026-08-27T210211Z-HyprLab
date: 2026-08-27T17:02:11-04:00
date_utc: 2026-08-27T210211Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: e72d84280
summary: Install wizard: detect audio sinks, play a test, persist default output
---

# Install wizard: detect audio sinks, play a test, persist default output

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `e72d84280` |
| When | 2026-08-27T17:02:11-04:00 |

## For the other machine

- [ ] On main: git pull. Optional: bash ~/.hyprgruv/lib/scripts/audio-setup.sh to pick speakers vs HDMI. Install wizard 04-config now includes this prompt.

## Notes

HyprLab currently has Speaker + three HDMI/DP sinks (docked). Wizard lists them, plays freedesktop audio-test-signal, then `pactl`/`wpctl set-default` plus a WirePlumber priority drop-in so HDMI does not steal the default on login.

`04-config.sh` prompt. Re-run: `bash ~/.hyprgruv/lib/scripts/audio-setup.sh`. `--apply` on Hyprland start.

Do not commit nord leftovers. Laptop ROLE stays deploy.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
