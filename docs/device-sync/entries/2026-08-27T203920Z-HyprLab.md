---
id: 2026-08-27T203920Z-HyprLab
date: 2026-08-27T16:39:20-04:00
date_utc: 2026-08-27T203920Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: d1e68ee8f
summary: Laptop staged idle: dim/lock/DPMS/hibernate + 5m lid grace; hypridle systemd unit; temporary HyprLab push
---

# Laptop staged idle: dim/lock/DPMS/hibernate + 5m lid grace; hypridle systemd unit; temporary HyprLab push

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `d1e68ee8f` |
| When | 2026-08-27T16:39:20-04:00 |

## For the other machine

- [ ] On main (source): git pull in ~/.hyprgruv. Desktop gets dim 13m → lock 14m → DPMS 15m → suspend 25m after: bash ~/.hyprgruv/lib/scripts/apply-machine-profile.sh desktop. Laptop already applied; still needs: sudo cp ~/.hyprgruv/lib/snapshot/99-hyprgruv-lid.conf /etc/systemd/logind.conf.d/99-hyprgruv-lid.conf && sudo systemctl kill -s HUP systemd-logind.

## Notes

Hypridle was not running (broken autostart one-liner). Lid close did nothing while logind reported Docked=true (`HandleLidSwitchDocked=ignore`).

Laptop chain: dim 4m → lock 5m → DPMS 6m → hibernate 15m.
Desktop chain: dim 13m → lock 14m → DPMS 15m → suspend 25m.
Lid (undocked): screens off immediately, 5 minutes unlocked, then lock + hibernate. Desk dock with an external display stays on.

hyprgruv-idle.service + hyprgruv-lid-watch.service. Laptop still needs the logind drop-in copied with sudo (no passwordless sudo on this apply).

Do not commit nord palette leftovers. Laptop ROLE stays deploy.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
