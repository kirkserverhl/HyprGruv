---
id: 2026-08-20T203744Z-HyprLab
date: 2026-08-20T16:37:44-04:00
date_utc: 2026-08-20T203744Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: 61e861552
summary: soundsbored wrapper probes HyprLab ~/Projects and main ~/BaaS_ISO; temporary laptop→main push
---

# soundsbored wrapper probes HyprLab ~/Projects and main ~/BaaS_ISO; temporary laptop→main push

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `61e861552` |
| When | 2026-08-20T16:37:44-04:00 |

## For the other machine

- [ ] On main (source): git pull in ~/.hyprgruv and ~/BaaS_ISO/soundsbored (or git-eod-pull). Super+Shift+A still uses ~/BaaS_ISO/soundsbored — no path change on the desktop. This laptop push is a temporary main-status exception; take source again after pull.

## Notes

Laptop (`HyprLab`) clone is `~/Projects/soundsbored`. Main desktop clone stays `~/BaaS_ISO/soundsbored`. Do not hardcode one path in the Hyprland wrapper.

Changes:
- `home/.config/hyprgruv/scripts/soundsbored.sh` — probe `~/Projects/soundsbored` then `~/BaaS_ISO/soundsbored`; prepend `.venv/bin` to `PATH`; `SOUNDSBORED_REPO` still overrides.
- `lib/git-sync/catalog.conf` — comment that main may still use `~/BaaS_ISO`.
- `soundsbored` repo (push separately): README documents both paths; `which()` finds venv `yt-dlp` without activating the environment.

Main desktop action: pull both repos. Super+Shift+A should keep working via `~/BaaS_ISO/soundsbored`. After pull, main is source again.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
