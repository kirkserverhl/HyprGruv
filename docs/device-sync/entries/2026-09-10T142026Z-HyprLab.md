---
id: 2026-09-10T142026Z-HyprLab
date: 2026-09-10T10:20:26-04:00
date_utc: 2026-09-10T142026Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: 668ca34e8
summary: fix hyprland.lua reload: flush Lua require cache, Lua blur dispatch, no shadow ~/.config/hypr, reload after git-eod-pull
---

# fix hyprland.lua reload: flush Lua require cache, Lua blur dispatch, no shadow ~/.config/hypr, reload after git-eod-pull

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `668ca34e8` |
| When | 2026-09-10T10:20:26-04:00 |

## For the other machine

- [ ] On main: cd ~/.hyprgruv && git pull && git-sync brief. Then hyprctl reload (or log out/in). Super binds should survive login hyprpm reload; the cannot-open-hyprland.lua overlay should not return after git-eod-pull.

## Notes

`git-eod-pull` on HyprLab stashed last night's uncommitted Super-key fix and rewrote `hyprland.lua` while Hyprland's watcher reloaded → overlay `cannot open …/hyprland.lua`.

Shipped in this commit:
- `hyprland.lua`: flush `package.loaded` for `colors*` + `conf.*` on every evaluation so Super binds re-register after login `hyprpm reload`.
- `apply-hypr-blur.sh`: `hl.dsp.force_renderer_reload()` instead of hyprlang `focuscurrentorlast` (Lua compile error overlay).
- `set_wallpaper.sh`: do not mkdir a shadow `~/.config/hypr`.
- `git-eod-pull.sh`: after hyprgruv pull, `ensure-hypr-config.sh` + delayed `hyprctl reload`.

Did **not** commit laptop-local hyprlock blur_passes, mimeapps, Thunar accels, or Obsidian cache.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
