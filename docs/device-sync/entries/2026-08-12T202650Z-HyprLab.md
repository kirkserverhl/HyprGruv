---
id: 2026-08-12T202650Z-HyprLab
date: 2026-08-12T16:26:50-04:00
date_utc: 2026-08-12T202650Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: 27a1cf933
summary: HyprLab: stop work-dock flip — LG stays left, laptop right. Dropped Lua auto/get_monitors race; apply via hyprctl eval after dock is visible.
---

# HyprLab: stop work-dock flip — LG stays left, laptop right. Dropped Lua auto/get_monitors race; apply via hyprctl eval after dock is visible.

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `27a1cf933` |
| When | 2026-08-12T16:26:50-04:00 |

## For the other machine

- [ ] cd ~/.hyprgruv && git pull && git-sync brief. Desktop monitor rules unchanged — HyprLab-only. No apply-machine-profile.

## Notes

- Last hook flipped the pair: Lua `monitor.added` / `config.reloaded` called `hl.get_monitors()` before the dock was listed, assumed undocked, and parked the laptop at `0x0`. Dock then landed on the right.
- Fix: static rules only in `monitors.lua` (LG `0x0`, laptop `2307x281`). No Lua event rebinds. Runtime pin is `apply-laptop-monitors.sh` via `hyprctl eval` (Lua config ignores `hyprctl keyword`).
- Laptop-only (`monitors_mode=laptop`) + serial `103MXTC4F409`. Desktop `desc:LG Electronics LG FULL HD` rules untouched.
- Docked: LG FULL HD left `0x0` scale `0.833333`; built-in `LG Display 0x061F` right `2307x281` scale `1.2`.
- Undocked: script moves the built-in to `0x0` only after the serial is confirmed gone.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
