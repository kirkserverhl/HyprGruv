---
id: 2026-08-13T202952Z-HyprLab
date: 2026-08-13T16:29:52-04:00
date_utc: 2026-08-13T202952Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: 1257e73eb
summary: HyprLab work-dock: both screens were stacked at 0x0 (cursor on both). Listener was bound to dead /tmp/hypr socket. Re-pinned LG left 0x0 / laptop 2304x281, fixed socket path, stop early-undock slam, Lua monitor.added backup.
---

# HyprLab work-dock: both screens were stacked at 0x0 (cursor on both). Listener was bound to dead /tmp/hypr socket. Re-pinned LG left 0x0 / laptop 2304x281, fixed socket path, stop early-undock slam, Lua monitor.added backup.

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `1257e73eb` |
| When | 2026-08-13T16:29:52-04:00 |

## For the other machine

- [ ] On main (source): cd ~/.hyprgruv && git pull && git-sync brief && git-sync init source

## Notes

- Symptom: HyprLab + work LG (serial `103MXTC4F409`) stacked both outputs at `0x0`. Cursor drew on both screens.
- Cause 1: `monitor-workspaces.sh` listened on `/tmp/hypr/.../socket2` (Hyprland 0.40+ uses `$XDG_RUNTIME_DIR/hypr/...`). Listener died at login, so hotplug never re-pinned.
- Cause 2: `apply-laptop-monitors.sh` treated a single early snapshot as undocked and slammed eDP to `0x0`. Dock then landed on top of it.
- Live pin now: dock `DP-2` `0x0` scale `0.833333` (left), laptop `eDP-1` `2304x281` scale `1.2` (right).
- Files: `apply-laptop-monitors.sh` (flock + poll before solo), `monitor-workspaces.sh` (XDG socket + reconnect), `autostart.lua` (`monitor.added`/`removed` script-only hooks, 2s login delay).
- Desktop / HyprGruv rules untouched. No apply-machine-profile.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
