---
id: 2026-08-12T202218Z-HyprLab
date: 2026-08-12T16:22:18-04:00
date_utc: 2026-08-12T202218Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: aced4dc8d
summary: HyprLab work-dock layout: when LG FULL HD serial 103MXTC4F409 is present, use dock 1920x1080@60 0x0 scale 0.833333 + laptop 1920x1080@60 2307x281 scale 1.2. Laptop-only (monitors_mode=laptop + serial match). Home desktop LG FULL HD is a different panel and is untouched.
---

# HyprLab work-dock layout: when LG FULL HD serial 103MXTC4F409 is present, use dock 1920x1080@60 0x0 scale 0.833333 + laptop 1920x1080@60 2307x281 scale 1.2. Laptop-only (monitors_mode=laptop + serial match). Home desktop LG FULL HD is a different panel and is untouched.

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `aced4dc8d` |
| When | 2026-08-12T16:22:18-04:00 |

## For the other machine

- [ ] On HyprGruv (source): cd ~/.hyprgruv && git pull && git-sync brief. Desktop monitor rules are unchanged — this is HyprLab-only. No apply-machine-profile needed.

## Notes

- File: `home/.config/hypr/conf/monitors.lua` — new `mode == "laptop"` branch only.
- Trigger: HyprLab + `monitors_mode=laptop` + dock serial `103MXTC4F409`.
- Docked: LG FULL HD @ `0x0` scale `0.833333`; laptop panel `LG Display 0x061F` @ `2307x281` scale `1.2`.
- Undocked: laptop panel @ `0x0` scale `1.2`.
- Desktop `mode == "desktop"` rules are unchanged. Do not re-apply this layout on HyprGruv.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
