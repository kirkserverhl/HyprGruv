---
id: 2026-08-27T205145Z-HyprLab
date: 2026-08-27T16:51:45-04:00
date_utc: 2026-08-27T205145Z
host: HyprLab
role: deploy
direction: to-source
branch: main
git_head: 0ae86b695
summary: Add kew, nm-applet, file thumbnailers to install lists; kew ~/Music setup
---

# Add kew, nm-applet, file thumbnailers to install lists; kew ~/Music setup

## Context

| Field | Value |
|-------|-------|
| Host | `HyprLab` |
| Role | `deploy` |
| Direction | `to-source` |
| Branch | `main` @ `0ae86b695` |
| When | 2026-08-27T16:51:45-04:00 |

## For the other machine

- [ ] On main: git pull in ~/.hyprgruv, then bash ~/.hyprgruv/sync-packages.sh (or pacman -S kew network-manager-applet kdegraphics-thumbnailers webp-pixbuf-loader). After pull, bash ~/.hyprgruv/lib/scripts/kew_setup.sh. hypremoji is AUR — already in aur.list; install with yay if missing.

## Notes

pacman.list: `kew`, `network-manager-applet`, `kdegraphics-thumbnailers`, `webp-pixbuf-loader`.
`hypremoji` was already in aur.list (comment clarified).
`kew_setup.sh` writes `~/.config/kew/kewrc` path=~/Music; hooked from 03-setup.sh.
nm-applet --indicator on Hyprland autostart.

kdegraphics-thumbnailers was the extra explicit package (with ffmpegthumbs) that paints image/PDF previews on files instead of generic icons.

Do not commit nord leftovers. Laptop ROLE stays deploy.

## Avoid overlaps

- Pull this handoff before editing the same areas on the other device.
- Device-local state stays in `~/.local/state/hyprgruv/` — do not commit it.
