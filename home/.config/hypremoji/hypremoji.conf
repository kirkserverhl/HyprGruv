# HyprEmoji (hypremoji) Configuration
#
# Themed via matugen → ~/.config/hypremoji/matugen.css
# (template: ~/.config/matugen/templates/hypremoji.css)
#
# Keybind is now handled in Hyprland config (SUPER + E)

# Old bind (kept for reference / other tools)
# bind = SUPER, period, exec, hypremoji

# Window rules for HyprEmoji (managed in hypr/conf/windowrules.lua):
# float + pin + center, 640x520 (calculator height; GTK will not go narrower).
