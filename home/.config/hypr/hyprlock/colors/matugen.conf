# Matugen Hyprlock Colors
# Generated from: Null
# This file is managed by Matugen — do not edit manually.
#
# Dedicated template for hyprlock so we can have lock-specific color tweaks
# without polluting the main hyprland colors.

# ============================================
# Core Material You Colors (same as hyprland)
# ============================================

$background = rgba(121318ff)
$on_background = rgba(e3e2e9ff)

$primary = rgba(b3c5ffff)
$on_primary = rgba(192e60ff)
$primary_container = rgba(324578ff)
$on_primary_container = rgba(dbe1ffff)

$secondary = rgba(c1c6ddff)
$on_secondary = rgba(2a3042ff)

$surface = rgba(121318ff)
$on_surface = rgba(e3e2e9ff)
$surface_variant = rgba(45464fff)
$on_surface_variant = rgba(c5c6d0ff)

$surface_container = rgba(1e1f25ff)
$surface_container_high = rgba(292a2fff)
$surface_container_highest = rgba(33343aff)

$outline = rgba(8f909aff)
$outline_variant = rgba(45464fff)

$error = rgba(ffb4abff)
$on_error = rgba(690005ff)

# Source color + tertiary (useful for accents)
$source_color = rgba(447bffff)
$icolor = $source_color

$tertiary = rgba(e2bbdcff)
$on_tertiary = rgba(422741ff)
$tertiary_container = rgba(5a3d58ff)

# ============================================
# Useful Aliases (for compatibility)
# ============================================

$bg = $background
$fg = $on_background
$text = $on_surface
$accent = $primary
$accent2 = $secondary

# Hyprbars / common UI friendly names
$bg0 = $surface
$bg1 = $surface_container
$bg2 = $surface_container_high

# Legacy / compatibility variables
$grey0 = rgba(45464fff)

# Some common accent names many configs expect
$red = $error
$green = $primary
$yellow = $secondary
$blue = $primary

# ============================================
# Extra convenience variables
# ============================================

$surface_dim = rgba(121318ff)
$surface_bright = rgba(38393fff)

# ============================================
# Hyprlock-specific variables (easy to customize here)
# ============================================

# Slightly more transparent card background for the lock screen input
$lock_input_bg = rgba(292a2fcc)

# Stronger accent for the lock screen
$lock_accent = $primary
