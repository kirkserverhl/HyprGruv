include colors/custom/matugen.conf

# Fonts
font_family      ShureTechMono Nerd Font
italic_font      auto
bold_font        auto
bold_italic_font auto
font_size        13.0
disable_ligatures never
allow_remote_control true
listen_on unix:@kitty-{kitty_pid}
background_opacity .70
# Let GrokNight / Tokyo Night TUI backgrounds pick up the same opacity (and
# Hyprland blur) as kitty's default background, so the canvas is not a solid
# rectangle inside the padded window.
transparent_background_colors #0e0e0e #1a1a1a #222222 #1a1b26 #1e202e #0a0a0a #140c09
cursor_shape block
cursor_blink_interval 0.5
cursor_stop_blinking_after 0
scrollback_lines 5000
scrollback_pager less +G -R
wheel_scroll_multiplier 1.0
click_interval 0.5
select_by_word_characters :@-./_~?&=%+#
mouse_hide_wait 2.0

# For a list of available layouts, see the file layouts.py
enabled_layouts *

remember_window_size   no
#initial_window_width   640
#initial_window_height  400

repaint_delay 8

input_delay 3

visual_bell_duration 0.0
enable_audio_bell no

open_url_with default

term xterm-kitty

window_margin_width 1

hide_window_decorations no

kitty_mod ctrl+shift

clear_all_shortcuts yes

clipboard_control write-clipboard write-primary

map kitty_mod+c      copy_to_clipboard
map kitty_mod+v      paste_from_clipboard

map kitty_mod+k         scroll_line_up
map kitty_mod+j         scroll_line_down

map kitty_mod+page_up   scroll_page_up
map kitty_mod+page_down scroll_page_down

map kitty_mod+home      scroll_home
map kitty_mod+end       scroll_end

map kitty_mod+left   resize_window narrower
map kitty_mod+right  resize_window wider
map kitty_mod+up     resize_window taller
map kitty_mod+down   resize_window shorter 3

map kitty_mod+enter launch --cwd=current

map kitty_mod+w close_window
map kitty_mod+] next_window
map kitty_mod+[ previous_window
map kitty_mod+r start_resizing_window

map kitty_mod+h next_tab
map kitty_mod+l previous_tab
map kitty_mod+t new_tab
map kitty_mod+q close_tab

map kitty_mod+t new_tab_with_cwd

tab_bar_margin_width      5
tab_bar_margin_height     5 0
tab_bar_style             separator
tab_bar_min_tabs          2
tab_separator             ""

map ctrl+c            copy_or_interrupt

map kitty_mod+plus      increase_font_size
map kitty_mod+minus     decrease_font_size
map kitty_mod+backspace restore_font_size

map kitty_mod+F11       toggle_fullscreen
map kitty_mod+F2        edit_config_file
map kitty_mod+F5        load_config_file

initial_width_width 78c
initial_window_height 23c
window_padding_width 22

sync_to_monitor no

cursor_trail 3

# Include theme
#include ~/colors/matugen.conf
# include ~/.cache/wal/colors-kitty.conf

# hide tabbar
# tab_bar_style hidden

